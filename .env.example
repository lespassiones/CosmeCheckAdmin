# ────────────────────────────────────────────────────────────
# Supabase — same project as the main CosmetWiki app.
# Service role key bypasses RLS — server-only, never exposed.
# ────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-jwt>
SUPABASE_SERVICE_ROLE_KEY=<service-role-jwt>

# ────────────────────────────────────────────────────────────
# Admin allowlist — comma-separated emails authorised to sign
# in to the dashboard. Anyone else is refused at the middleware
# layer even if they hold a valid Supabase session.
# ────────────────────────────────────────────────────────────
ADMIN_EMAILS=brianbiendou@gmail.com

# ────────────────────────────────────────────────────────────
# Programme bêta — le bouton « Envoyer les invitations » appelle
# le main app (POST /api/beta/invite). BETA_INVITE_SECRET doit
# être IDENTIQUE à celui du main app (CosmetWiki).
# ────────────────────────────────────────────────────────────
BETA_MAIN_APP_URL=https://www.cosme-check.com
BETA_INVITE_SECRET=

# ────────────────────────────────────────────────────────────
# Canonical URL used by next/headers redirects.
# In prod set this to https://admin.cosme-check.com or whatever
# Vercel domain you assign.
# ────────────────────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Clé admin OpenAI (sk-admin-…) pour l API Costs — admin uniquement
OPENAI_ADMIN_KEY=
